import { SpringValue } from "react-spring";
export declare type Position = {
    x: number;
    y: number;
};
export declare type SpringPosition = {
    x: SpringValue<number>;
    y: SpringValue<number>;
};
